/** @jsxImportSource theme-ui */
import React from 'react';

import { Spinner } from 'theme-ui';

const Loader = () => (
   <Spinner
      sx={{
         color: 'bee',
         margin: 'auto',
      }}
   />
);

export default Loader;
